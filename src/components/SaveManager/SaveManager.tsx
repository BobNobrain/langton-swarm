import type { Component } from 'solid-js';
import styles from './SaveManager.module.css';

export const SaveManager: Component = () => {
    return <div class={styles.manager}>Saves are under construction</div>;
};
