import type { Component } from 'solid-js';
import styles from './GameSettings.module.css';

export const GameSettings: Component = () => {
    return <div class={styles.manager}>Game settings are under construction</div>;
};
